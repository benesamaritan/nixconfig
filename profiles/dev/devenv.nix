{ pkgs, inputs, ... }:

{
  # This is the "global" dev profile configuration that can be used 
  # to define a default devenv shell if needed, but per the request 
  # to "Ensure development pkgs available only on devshell", we 
  # can define these in a shell context.
}
